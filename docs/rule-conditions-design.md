# Условия правил: назначение, домены, списки правил и DNS

Статус: этапы 3 и 4 сделаны — условия по назначению, списки `.srs`, их
редактирование на странице и маршрутизация DNS (раздел 17). Документ продолжает
[`docs/routing-design.md`](routing-design.md) и раскрывает то, что там названо
этапами 3 и 4 (раздел 13.1). Модель, инварианты И1–И5, схема UCI и
механика применения оттуда действуют здесь без изменений; ниже описано только
то, что добавляется.

Пользователей у Zarap по-прежнему нет, поэтому миграции не будет:
`/etc/config/zarap` принимает новый вид, а установленные конфигурации
переустанавливаются.

## 1. Что добавляется

Сегодня правило умеет ровно одно условие — список устройств, — и потому отвечает
только на вопрос «кто». Куда именно идёт трафик, правило сказать не может, и
единственный способ отправить в прокси один сайт, а не всё устройство, — это
переключить `main.final` и перечислить исключения, то есть вывернуть настройку
наизнанку.

Добавляются условия по назначению:

- **домены** — `youtube.com` и всё, что под ним;
- **диапазоны адресов** — `149.154.160.0/20`;
- **списки правил** (`.srs`) — готовые наборы доменов и подсетей, geosite/geoip;
- **порты и протокол** — `443`, `udp`.

И, отдельным этапом, **DNS-маршрутизация**: резолв доменов, названных правилами,
уезжает в прокси, вместо того чтобы идти к провайдеру.

Главное требование к форме: одно правило должно уметь и то, и другое, и оба
вместе. «Телевизор — целиком через Нидерланды», «весь дом — youtube через
Нидерланды», «телевизор — youtube через Нидерланды, остальное напрямую» — это три
правила одного вида, а не три разных механизма.

## 2. Модель: правило — это «кто» И «куда»

Условия правила делятся на три группы, и группы ведут себя по-разному. Это
единственное место документа, которое обязательно понять до всего остального.

| Группа | Опции | Смысл | Как складываются |
| --- | --- | --- | --- |
| Источник | `client` | «трафик одного из этих устройств» | ИЛИ внутри списка |
| Назначение | `domain`, `ip_cidr`, `rule_set` | «трафик к одному из этих мест» | ИЛИ внутри всей группы, включая разные виды |
| Уточнение | `port`, `network` | «и при этом такой порт / такой протокол» | И ко всему остальному |

Между группами — И. Правило срабатывает, когда трафик пришёл от одного из
названных устройств **и** идёт к одному из названных мест **и** удовлетворяет
уточнениям.

Пустая группа означает «любой»: правило без устройств действует на весь LAN,
правило без назначения — на весь трафик названных устройств. Пустыми могут быть
любые две из трёх, но не все три сразу (раздел 9).

### 2.1 Три формы правила

```
# только устройства — то, что умеет сегодняшний Zarap
list client '00:11:22:33:44:55'
option target 'out_1'

# только назначение — действует на весь дом
list domain 'youtube.com'
option target 'out_1'

# и то и другое — телевизор ходит на youtube через прокси, остальное напрямую
list client '00:11:22:33:44:55'
list domain 'youtube.com'
option target 'out_1'
```

### 2.2 Почему назначение складывается через ИЛИ

Внутри одного правила sing-box соединяет разные поля через И: правило с
`domain` и `rule_set` сразу требует, чтобы имя совпало **и** с доменом, **и** с
элементом списка, и не срабатывает практически никогда. Между тем человек,
который вписал в одно правило домен и список, имеет в виду ровно обратное:
«вот это, а также вот это».

Поэтому одна секция `rule` перестаёт давать ровно один элемент `route.rules` —
она даёт **группу подряд идущих элементов с одной целью**, по одному на вид
условия назначения (раздел 5.1). Соседство в списке правил и есть ИЛИ: первое
совпавшее выигрывает, а цель у всех элементов группы одна, так что какой именно
из них совпал — безразлично.

Логические правила sing-box (`"type": "logical"`) рассмотрены и отвергнуты. Они
сохранили бы соответствие «одна секция — один элемент», но ценой вложенности,
в которой отказ `sing-box check` перестаёт указывать на конкретное условие, и
ценой второй формы записи действия, которую пришлось бы отдельно проверять на
каждой версии sing-box. Плоская группа читается в конфиге глазами и разбирается
тестом построчно.

## 3. Инварианты

К И1–И5 из `routing-design.md` добавляются три. Они дописывают тот же список и
так же не подлежат нарушению ради удобства.

**И6. Kill switch по-прежнему берётся только из `client`.** Набор
`zarap_guarded_v4` заполняется устройствами, названными правилами, и ничего не
знает ни о доменах, ни о списках. Правило, ограниченное доменом, ставит
названное устройство под kill switch **целиком** — гарантия остаётся свойством
устройства, а не потока.

Обоснование — в вопросе, на который kill switch отвечает: «даёт ли выключение Zarap
этому устройству прямой выход в интернет». Ответ обязан быть «нет» для всякого
устройства, о котором кто-то написал правило, независимо от того, какую часть его
трафика правило описывает. Обратное решение — снимать защиту, когда правило
касается только части трафика, — означало бы, что при выключенном Zarap
устройство уходит в WAN напрямую в том числе теми соединениями, ради которых
правило и писалось.

**И7. Правило без устройств не защищает никого.** Условие по назначению не
называет устройство, значит в набор kill switch от него не попадает ничего.
Правило «весь дом — youtube через прокси» маршрутизирует, пока Zarap включён, и
не оставляет следа, когда он выключен. Это то же свойство, что и у `main.final`,
и интерфейс обязан показывать его так же прямо.

**И8. Условия по назначению ничего не меняют в nftables.** Захват остаётся
сплошным по LAN, разбор назначения целиком принадлежит sing-box. Соблазн не
заворачивать в TProxy трафик, который заведомо пойдёт в `direct`, — это И3 в
новой одежде, и он так же неверен: обход открыл бы прямой путь через forward и
оставил бы его открытым при остановленном sing-box.

## 4. Схема UCI

### 4.1 Секция `rule`

```
config rule
	list client '00:11:22:33:44:55'
	list domain 'youtube.com'
	list domain '.googlevideo.com'
	list ip_cidr '149.154.160.0/20'
	list rule_set 'rs_1'
	list port '443'
	option network 'tcp'
	option target 'out_1'
```

| Опция | Тип | Смысл |
| --- | --- | --- |
| `client` | list | MAC-адреса; источник |
| `domain` | list | имя целиком или суффикс (4.2) |
| `ip_cidr` | list | IPv4-подсеть или адрес |
| `rule_set` | list | имена секций `ruleset` |
| `port` | list | порт или диапазон `1000:2000` |
| `network` | option | `tcp`, `udp` или пусто — оба |
| `target` | option | `out_<n>`, `direct` или `block` |

Секции по-прежнему анонимные, порядок в файле — это и есть порядок правил, и
`configure_uci()` по-прежнему переписывает весь набор целиком. Ни имени, ни
выключателя у правила не появляется: причины, записанные в разделе 4
`routing-design.md`, от новых условий не меняются.

### 4.2 Домен и его поддомены — одна запись

`youtube.com` человек пишет, имея в виду и `www.youtube.com`, и
`m.youtube.com`. Поэтому одно поле, а не три (`domain`, `domain_suffix`,
`domain_keyword`), и одно правило чтения:

- `youtube.com` → само имя **и** всё под ним;
- `.googlevideo.com` → только поддомены, само имя не совпадает;
- всё остальное (`*`, `://`, путь, пробел) → ошибка ввода.

`domain_keyword` и `domain_regex` в схему не заводятся. Совпадение по куску
имени — это способ случайно завернуть в прокси половину интернета (`vk` внутри
`vkontakte.ru` и внутри `invknews.example`), а разбирать потом, почему сайт не
открывается, придётся по журналу.

### 4.3 Секции `ruleset`

```
config zarap 'main'
	option ruleset_detour 'out_1'

config ruleset 'rs_1'
	option label 'Заблокированное в РФ'
	option url 'https://example.org/geosite-ru-blocked.srs'
	option update_interval '1d'
```

Именованные, `rs_<n>`, где `n` — наименьшее свободное число; имя выдаёт
страница, ровно по той же причине, по которой она выдаёт `out_<n>`: правило
ссылается на список до того, как что-либо применено. Имя обязано соответствовать
`^rs_[0-9]+$`; проверять нужно и на входе, и при чтении, потому что
`/etc/config/zarap` можно отредактировать руками.

Секция — целиком описание того, что сделает sing-box: откуда взять файл и как
часто обновлять. Своей копии файла Zarap не держит и ничего о его содержимом не
знает (раздел 8).

`main.ruleset_detour` — подключение, через которое идёт загрузка, или `direct`.
Оно одно на все списки: источники списков (GitHub, jsDelivr) в сетях, ради
которых Zarap написан, недоступны ровно так же, как и всё остальное, и ответ на
вопрос «через что за ними идти» у них общий. Держать его в каждой секции значило
бы просить один и тот же ответ столько раз, сколько списков заведено, и
позволять им разойтись без причины. Значение по умолчанию — `direct`, но
интерфейс поднимает первый заведённый список на первое объявленное подключение:
загрузка напрямую там просто не состоится.

Прежняя схема держала `option detour` в каждой секции `ruleset`. Оно не
читается: конфигурация с той схемы возвращается к `direct`, и подключение
выбирается заново — одно действие на странице против кода миграции, который
пришлось бы носить дальше. Секции переписываются целиком на каждом применении,
поэтому старое поле исчезает вместе с первым сохранением.

`update_interval` — как часто sing-box перекачивает список: `1d`, `12h`, `168h`.
Пусто означает «не обновлять», и это законный выбор для списка, который меняется
раз в год.

## 5. Преобразование в конфиг sing-box

### 5.1 `route.rules[]`

Одна секция `rule` даёт группу элементов: по одному на непустой вид условия
назначения, и один элемент, если условий назначения нет вовсе. Источник,
уточнения и цель повторяются в каждом элементе группы.

```
config rule
	list client '00:11:22:33:44:55'
	list domain 'youtube.com'
	list ip_cidr '149.154.160.0/20'
	list rule_set 'rs_1'
	list port '443'
	option target 'out_1'
```

```json
[
  {
    "inbound": ["zarap-tproxy"],
    "source_ip_cidr": ["192.168.1.50/32"],
    "port": [443],
    "domain": ["youtube.com"],
    "domain_suffix": [".youtube.com"],
    "outbound": "out_1"
  },
  {
    "inbound": ["zarap-tproxy"],
    "source_ip_cidr": ["192.168.1.50/32"],
    "port": [443],
    "ip_cidr": ["149.154.160.0/20"],
    "outbound": "out_1"
  },
  {
    "inbound": ["zarap-tproxy"],
    "source_ip_cidr": ["192.168.1.50/32"],
    "port": [443],
    "rule_set": ["rs_1"],
    "outbound": "out_1"
  }
]
```

Правила чтения записей:

| Запись в UCI | Поле sing-box |
| --- | --- |
| `youtube.com` | `domain: ["youtube.com"]` и `domain_suffix: [".youtube.com"]` |
| `.googlevideo.com` | `domain_suffix: [".googlevideo.com"]` |
| `149.154.160.0/20` | `ip_cidr: ["149.154.160.0/20"]` |
| `93.184.216.34` | `ip_cidr: ["93.184.216.34/32"]` |
| `443` | `port: [443]` |
| `1000:2000` | `port_range: ["1000:2000"]` |
| `network 'udp'` | `network: ["udp"]` |

Отображение `target` не меняется: `out_<n>` даёт `outbound`, `direct` даёт
`outbound: "direct"` и объявление direct-outbound, `block` даёт
`action: "reject"`. При `block` действие ставится в каждом элементе группы.

### 5.2 `action: "sniff"` — когда и зачем

TProxy-inbound видит IP и порт, но не имя: клиент уже сходил в DNS сам.
Чтобы `domain` и доменная часть `rule_set` вообще могли совпасть, в начало
`route.rules` добавляется правило снифинга:

```json
{ "inbound": ["zarap-tproxy"], "action": "sniff" }
```

Оно вытаскивает имя из TLS ClientHello (SNI), из заголовка `Host` и из
QUIC-рукопожатия. Старый путь через `inbound.sniff` объявлен устаревшим в
sing-box 1.11.0 и удалён в 1.13.0, поэтому сразу пишется действие правила.

Правило выпускается **только если** хотя бы в одном правиле есть `domain` или
`rule_set` — по той же логике, по которой direct-outbound объявляется только
при ссылке на него. Снифинг не бесплатен: он задерживает первый пакет
соединения на время ожидания заголовка, и платить за это, когда доменных правил
нет, незачем.

### 5.3 `rule_set[]` и кэш

Списки объявляются удалёнными: качает и обновляет их сам sing-box.

```json
"route": {
  "rule_set": [
    {
      "type": "remote",
      "tag": "rs_1",
      "format": "binary",
      "url": "https://example.org/geosite-ads.srs",
      "download_detour": "out_1",
      "update_interval": "1d"
    }
  ]
},
"experimental": {
  "cache_file": { "enabled": true, "path": "/etc/zarap/cache.db" }
}
```

`experimental.cache_file` — не украшение, а условие работоспособности: без него
скачанный список живёт в памяти процесса, и каждый перезапуск sing-box
превращается в поход в сеть. С кэшем перезагрузка роутера поднимает списки с
диска, а сеть нужна только на обновление по `update_interval`. Путь ведёт в
`/etc/zarap/`, потому что кэш обязан пережить перезагрузку; `/tmp` вернул бы ту
же зависимость от сети на каждом старте.

Секция `experimental` выпускается **только когда объявлен хотя бы один список** —
по той же логике, по которой direct-outbound объявляется только при ссылке на
него. Без списков кэшу нечего хранить, а лишний файл на flash не нужен.

`download_detour` берётся из `main.ruleset_detour` и пишется в каждое
объявление: своего места для «через что качать вообще» в схеме sing-box нет.
Опускается при `direct`. Поле объявлено устаревшим в
sing-box 1.14.0 в пользу `http_client`; когда до OpenWrt доедет такая версия,
`sing-box check` скажет об этом первым (И5), и замена сведётся к одной ветке в
генераторе.

Разбор того, чем за это плачено и что происходит при недоступности источника, —
в разделе 8.

### 5.4 Пример целиком

Телевизор ходит на YouTube через Нидерланды, а всё остальное — напрямую; весь
дом не ходит в рекламные сети; QUIC заблокирован для всех, чтобы браузеры не
обходили снифинг:

```
config zarap 'main'
	option enabled '1'
	option final 'direct'
	option ruleset_detour 'out_1'

config outbound 'out_1'
	option label 'Нидерланды'
	…

config ruleset 'rs_1'
	option label 'Реклама'
	option url 'https://example.org/geosite-ads.srs'
	option update_interval '1d'

config rule
	list port '443'
	option network 'udp'
	option target 'block'

config rule
	list rule_set 'rs_1'
	option target 'block'

config rule
	list client '00:11:22:33:44:55'
	list domain 'youtube.com'
	list domain '.googlevideo.com'
	option target 'out_1'
```

```json
{
  "log": { "level": "info", "timestamp": true },
  "inbounds": [
    { "type": "tproxy", "tag": "zarap-tproxy", "listen": "0.0.0.0", "listen_port": 7893 }
  ],
  "outbounds": [
    { "type": "vless", "tag": "out_1", "server": "nl.example.net", … },
    { "type": "direct", "tag": "direct" }
  ],
  "route": {
    "auto_detect_interface": true,
    "rule_set": [
      {
        "type": "remote", "tag": "rs_1", "format": "binary",
        "url": "https://example.org/geosite-ads.srs",
        "download_detour": "out_1", "update_interval": "1d"
      }
    ],
    "rules": [
      { "inbound": ["zarap-tproxy"], "action": "sniff" },
      { "inbound": ["zarap-tproxy"], "network": ["udp"], "port": [443], "action": "reject" },
      { "inbound": ["zarap-tproxy"], "rule_set": ["rs_1"], "action": "reject" },
      {
        "inbound": ["zarap-tproxy"],
        "source_ip_cidr": ["192.168.1.50/32"],
        "domain": ["youtube.com"],
        "domain_suffix": [".youtube.com", ".googlevideo.com"],
        "outbound": "out_1"
      }
    ],
    "final": "direct"
  },
  "experimental": {
    "cache_file": { "enabled": true, "path": "/etc/zarap/cache.db" }
  }
}
```

Под kill switch здесь одно устройство — телевизор, названный третьим правилом.
Первые два правила не называют никого и при выключенном Zarap не действуют вовсе
(И7).

DNS-половина этого же конфига — секция `dns` для домена из третьего правила и
`default_domain_resolver` для домена в адресе самого сервера — показана
отдельно, в 7.2 и 7.6: здесь этап 3, который DNS ещё не трогает.

## 6. Домены без DNS: что работает и что нет

Этап 3 не трогает DNS вовсе. Клиенты продолжают резолвить через dnsmasq, тот
ходит к провайдеру, и ранний `return` по `zarap_direct_v4` выводит этот путь
из-под захвата. Маршрутизация по доменам при этом работает, потому что имя
берётся из снифинга, а не из DNS. Границы у такой схемы честные, и их надо
знать до реализации, а не после.

**Работает:** HTTPS и HTTP, включая QUIC, то есть практически всё, чем занят
браузер и большинство приложений.

**Не работает:**

- соединение, в котором имени нет вовсе, — голый IP, произвольный TCP, VPN
  внутри VPN. Доменное правило до него не дотягивается, и трафик уходит по
  `final`;
- Encrypted Client Hello: SNI зашифрован, снифинг не видит имени. Пока ECH
  распространён мало, но это направление, в котором интернет движется;
- **любая блокировка, устроенная в DNS.** И NXDOMAIN, и подменённый адрес.

Последний пункт стоил этому разделу переписывания. До проверки здесь было
написано, что подмена адреса безвредна: соединение откроется, имя приедет в
ClientHello, и наружу пойдёт домен, который резолвит уже сервер. Проверка (15.2)
показала обратное: в outbound уходит `outbound connection to 8.6.112.0:443` —
тот самый адрес, который клиент получил от провайдерского DNS. Имя используется
только чтобы выбрать маршрут.

Значит, доменное правило меняет **путь**, но не **точку назначения**. Если DNS
выдал правильный адрес, а мешает DPI по SNI, — снифинг решает задачу целиком.
Если DNS выдал заглушку, соединение уедет в прокси и оттуда постучится в ту же
заглушку.

Переключить это поведение нечем: устаревшее `sniff_override_destination`
принимается и не действует (15.2).

Отсюда две вещи. Этап 4 перестаёт быть удобством: он единственное, что чинит
блокировки в DNS, — и чинит их именно так, как нужно при этом поведении, потому
что клиент получает настоящий адрес и дальше всё работает само. А интерфейс
обязан сказать пользователю, что доменное правило — против DPI, а не против
подменённого DNS, и что сами DNS-запросы по-прежнему видны провайдеру.

## 7. DNS-маршрутизация (этап 4)

### 7.1 Что решается

Две вещи: домены, которые местный DNS отказывается резолвить, и сам факт того,
что список посещаемых сайтов уходит провайдеру открытым текстом.

### 7.2 Схема: dnsmasq остаётся, sing-box получает названные домены

```
клиент → dnsmasq :53
           ├── домены из правил → sing-box :5353 → DoH через out_N
           └── всё остальное    → апстрим провайдера, как сегодня
```

Для каждого домена, названного правилом с целью `out_<n>`, в `/etc/config/dhcp`
пишется строка вида `list server '/youtube.com/127.0.0.1#5353'`. dnsmasq умеет
адресную пересылку по домену штатно, и это ровно то, что нужно: перенаправляется
именно названное, а не весь DNS дома.

sing-box со своей стороны получает DNS-слушателя на `127.0.0.1:5353` — inbound
типа `direct` с правилом `{"inbound": ["dns-in"], "action": "hijack-dns"}`. Это
было самым слабым местом всего раздела: inbound такого типа объявляли
устаревшим, и его отсутствие переписало бы схему целиком. На 1.12.17 он принят
(15.1), так что этап 4 строится на том, что есть.

Конфигурация, в которой резолвером по умолчанию служит DoH через тот же
outbound, куда правило отправляет трафик:

```json
"dns": {
  "servers": [
    { "type": "https", "tag": "dns_out_1", "server": "1.1.1.1", "detour": "out_1" }
  ],
  "rules": [
    { "domain_suffix": [".youtube.com"], "server": "dns_out_1" }
  ],
  "strategy": "ipv4_only",
  "final": "dns_out_1"
}
```

По одному DNS-серверу на каждый outbound, названный доменными правилами: ответ
должен приходить с того же выхода, куда пойдёт соединение, иначе геобалансировка
CDN разведёт их по разным континентам.

Форма записи — типизированная, введённая в 1.12.0. Старая, с `address`, тоже
принимается установленной версией (15.1), но писать надо современную: она и есть
то, во что старая мигрирует, а расхождение со схемой поймает `sing-box check`
(И5).

`strategy: "ipv4_only"` обязателен и не является настройкой: IPv6 в LAN не
раздаётся (раздел 7 `routing-design.md`), и AAAA-ответ отправил бы клиента по
адресу, до которого он не дойдёт.

### 7.3 Почему только домены, а не весь DNS

Перенаправить в sing-box **весь** DNS выглядит проще и заманчивее — и это
ошибка того же рода, что и захват всего LAN, только без выкупа. Радиус отказа:
при остановленном sing-box дом теряет не часть маршрутов, а имена целиком,
включая имена в самой локальной сети. Плюс появляется вопрос, куда sing-box
отправляет **не**проксируемые запросы: `local`-резолвер на OpenWrt смотрит в
`/etc/resolv.conf`, а тот указывает на dnsmasq, который только что переслал
запрос в sing-box, — это петля. Обходить её пришлось бы вычитыванием апстрима
провайдера из ubus и запеканием его в конфиг, который не переживёт
переподключения WAN.

Адресная пересылка по домену снимает оба вопроса разом: sing-box не видит
ничего, кроме названных доменов, и не нуждается в прямом резолвере вовсе.

Плата — списки `.srs` DNS-маршрутизацией не покрываются: тысячи доменов из
geosite в dnsmasq не перечислить. Для правил по спискам DNS остаётся прямым, а
маршрут по-прежнему даёт снифинг. Это записанная граница, а не недоделка.

### 7.4 Отвергнутые варианты

- **`action: "hijack-dns"` на TProxy-inbound.** Перехватывать нечего: запросы
  клиентов идут к самому роутеру и выводятся из захвата ранним `return` по
  `zarap_direct_v4`, а исходящие запросы dnsmasq рождаются на роутере и в
  prerouting не попадают вовсе.
- **`fakeip`.** Снимает зависимость от снифинга, но ломает `ip_cidr`-правила,
  ломает всё, что резолвит имена не через роутер, и требует состояния, которое
  надо где-то держать и как-то чистить.
- **DoH-клиент на роутере отдельным пакетом.** Резолвил бы мимо прокси, то есть
  решал бы приватность и не решал бы блокировки.

### 7.5 Чего это стоит

`/etc/config/dhcp` правится ещё в одном месте — тем же транзакционным путём,
которым уже идут статические аренды и выключение RA, и с тем же восстановлением
при удалении пакета. Секции `server`, написанные не Zarap, не трогаются: свои
помечаются так же, как помечаются аренды.

### 7.6 Bootstrap: имена, которые роутер набирает сам

Всё выше — про имена, которые спрашивает клиент. Есть и вторые: те, что роутер
набирает сам, ещё до всякого клиента.

- адрес сервера из ссылки, когда он записан доменом, а не адресом;
- хост источника списка `.srs`, когда список качается напрямую, а не через
  подключение (через подключение имя уезжает внутрь него, и резолвить его на
  роутере не надо).

Резолвить их через прокси нельзя, и это не вопрос вкуса, а круг: чтобы
дозвониться до подключения, надо знать адрес его сервера, а чтобы узнать
адрес — дозвониться до подключения. Ровно это и происходило: с
`dns.final: "dns_out_1"` (7.2) sing-box на каждом соединении писал
`dns: lookup domain nl.example.net`, шёл за ответом в тот самый outbound, снова
упирался в то же имя — и так без конца, молча, без единой ошибки в журнале.
Ссылка с доменом в хосте не работала вовсе, а ссылка с адресом — работала, и
разница ничем не объяснялась.

Поэтому в `dns.servers` добавляется ещё один сервер, единственный, кто резолвит
мимо туннеля:

```json
"dns": {
  "servers": [
    { "type": "https", "tag": "dns_bootstrap", "server": "1.1.1.1" }
  ]
},
"route": {
  "default_domain_resolver": "dns_bootstrap"
}
```

Три решения, каждое проверено на sing-box 1.12.17 (15.4).

**`detour` не указан.** Не потому что забыли: `detour: "direct"` sing-box
отвергает прямым текстом — `detour to an empty direct outbound makes no sense`,
— а сервер без `detour` дозванивается напрямую, а не по `route.final`. Проверено
запуском с `final: "out_1"`: запрос ушёл мимо outbound'а. Прямой outbound от
этого объявлять не нужно, и правило «`direct` объявляется только когда на него
кто-то указывает» остаётся в силе.

**`route.default_domain_resolver`, а не правило в `dns.rules`.** Названный так
резолвер спрашивается напрямую, `dns.rules` при этом не просматриваются —
проверено конфигом, где имя сервера не совпадало ни с одним правилом, а
`dns.final` вёл в туннель: запрос всё равно ушёл в bootstrap. Значит перечислять
адреса серверов ещё и в `dns.rules` не надо. Поле появилось в 1.12.0; без него
sing-box берёт `dns.final`, и это поведение объявлено устаревшим (`check`
знает его как `missing-domain-resolver`).

**DoH, а не обычный DNS и не `local`.** Имя сервера подменяют ровно там, где
Zarap и нужен, и подменённый ответ увёл бы подключение в никуда, поэтому запрос
шифруется. `local` (то есть `/etc/resolv.conf`, то есть dnsmasq) отвергнут по той
же причине, что и в 7.3, только острее: dnsmasq здесь не чужой, Zarap сам пишет
в него пересылку, и стоит правилу назвать домен, совпавший с адресом сервера,
как получается петля dnsmasq → sing-box → dnsmasq. С отдельным резолвером по
адресу петля невозможна структурно. Адресом указан сам резолвер (`1.1.1.1`),
поэтому резолвить для него нечего — сертификат Cloudflare содержит IP SAN.

Цена записана честно: если провайдер закрывает `1.1.1.1:443`, имя сервера не
разрешится и подключение не поднимется. Обход — указать в ссылке адрес вместо
имени; тогда bootstrap не понадобится вовсе и в конфиг не попадёт.

Признак отказа в журнале — строка вида
`... using outbound/vless[out_1]: lookup nl.example.net: ...`; по ней статус
переходит в `connection_error`, а не остаётся «работает» при неработающем
прокси.

## 8. Списки `.srs`

### 8.1 Качает и обновляет sing-box

Секция `ruleset` — это описание, а не файл. Zarap не скачивает список, не хранит
его копию и не знает, что внутри: он пишет в конфиг адрес, интервал и общее для
всех списков подключение, а всё остальное делает sing-box — там уже есть и
загрузчик с ETag, и кэш, и фоновое обновление по расписанию.

Своя загрузка была бы вторым механизмом для одного и того же и, что хуже,
механизмом ущербным: бэкенд ходит в сеть от имени роутера, а трафик роутера
захватом не покрывается (раздел 6.1 `routing-design.md`) — значит, за списком он
шёл бы напрямую, мимо прокси, ровно там, где источник и заблокирован. У sing-box
такой проблемы нет: `download_detour` отправляет загрузку через подключение,
которое пользователь уже настроил.

Отсюда же — соотношение обязанностей: Zarap отвечает за то, чтобы конфигурация
была корректной и применялась транзакционно, sing-box — за содержимое списков.
Ни один слой не делает работу другого.

### 8.2 Кэш, flash и первое применение

Кэш (`experimental.cache_file`, 5.3) — то, что превращает «скачать один раз» в
«скачать один раз навсегда». Он лежит в `/etc/zarap/cache.db`, переживает
перезагрузку и растёт вместе со списками: geosite целиком — это мегабайты, а
`/overlay` на домашнем роутере невелик.

Поэтому:

- интерфейс показывает размер `cache.db` рядом со списками — единственное
  честное число, которое здесь есть, потому что размер конкретного списка
  Zarap неоткуда взять;
- `status` возвращает свободное место на `/overlay`, и страница предупреждает,
  когда его остаётся меньше 10 %;
- предупреждение приходит **до** того, как место кончится: порог не в процентах,
  а абсолютный — меньше 8 МиБ свободного. На проверенном роутере `/overlay`
  всего 44 МиБ (15.1), и 10 % от них — это 4,4 МиБ, то есть предупреждение
  пришло бы, когда для geosite-набора уже поздно;
- дальше защиты нет: что сделает sing-box, когда кэш станет некуда писать —
  продолжит работать без него или откажется стартовать, — остаётся непроверенным
  (15.2).

Первое применение с новым списком — единственный момент, когда всё зависит от
сети: файла в кэше ещё нет. Проверять доступность источника заранее бэкенд не
может (см. 8.1), поэтому проверка происходит там же, где проверяется всё
остальное, — на запуске службы, существующим путём: `apply` стартует sing-box,
`check_runtime()` не досчитывается процесса или TProxy-порта, и вся конфигурация
откатывается с сообщением. Это работает уже сегодня и на списки распространяется
без единой новой строки.

Что нужно добавить — текст ошибки. Общее «не удалось запустить Zarap» при
недоступном списке отправляет искать проблему не туда, поэтому при откате с
объявленными списками сообщение обязано называть подозреваемого и предлагать
проверить адрес и подключение, выбранное для загрузки списков.

### 8.3 Обновление и отказ источника

Обновление — забота sing-box, по `update_interval`, через то же подключение. Но
интервала хватает не всегда: список правят на источнике, и ждать сутки нечего;
роутер стоял без интернета, когда подходил срок. Поэтому есть и «обновить
сейчас» — метод `refresh_rulesets` и кнопка под таблицей списков.

Команды «сходи за списками прямо сейчас» у sing-box нет — ручное обновление
живёт в экспериментальном Clash API, и тянуть в пакет целую подсистему ради
одной кнопки по-прежнему не стоит того. Зато есть рычаг попроще: старт с кэшем
берёт список из кэша, а старт без кэша идёт в сеть за каждым. Обновление — это
и есть убрать кэш и перезапустить службу.

Отсюда две вещи, которые кнопка обязана делать честно.

Первая — обновляются все списки сразу. `cache.db` внутри принадлежит sing-box,
вынуть из него один набор нечем, и построчная кнопка обещала бы то, чего под ней
нет. Это тот же ответ, что и у выбора подключения (8.1): один на весь роутер.

Вторая — кэш откладывается, а не удаляется. Старт без кэша зависит от чужого
сервера (риск 6), и если списка нет ни в кэше, ни в сети, служба может не
подняться: обновление списка стоило бы дому интернета. Поэтому `cache.db`
переименовывается в `cache.db.bak` — переименование, а не копия, лишнего места
на flash оно не просит, — и при неудачном запуске возвращается на место, после
чего sing-box стартует снова на прежних списках. Пользователь читает, что
обновление не состоялось, вместе с тем же указанием на журнал, что и при откате
применения (8.2).

Выключенный Zarap перезапускать нечем: sing-box под его конфигурацией не
работает. Там кэш просто удаляется, а за списками служба сходит на следующем
запуске, и ответ говорит именно это.

Чего кнопка не умеет — сказать, что список действительно скачался. Файл
принадлежит sing-box, Zarap в него не смотрит, и всё, что он проверяет, — это
что служба поднялась и прошла ту же проверку, что после применения. Пока
неизвестно, отказывается ли sing-box стартовать без списка или работает без
него (15.3), «скачаны заново» остаётся утверждением о запуске без кэша, а не о
содержимом файла; ошибка загрузки видна в журнале.

Отказ источника после первой удачной загрузки безвреден: в кэше лежит прошлая
версия, sing-box продолжает работать на ней и повторит попытку позже. Опасен
только отказ **до** первой удачной загрузки — и он разобран в 8.2.

Отдельный случай — формат. `.srs` версионируется вместе с sing-box, и список,
собранный для более новой версии, установленный бинарник не прочтёт. Zarap
поймать это не может: файла он не видит. Ошибка проявится как отказ запуска и
уедет в откат, а разбираться придётся по журналу — там sing-box пишет и адрес
списка, и причину. Это принятая цена решения (риск 7).

Загрузка файлом с компьютера — естественное дополнение для случая, когда URL
недоступен ниоткуда, но не для первой итерации: она требует и отдельного пути
загрузки в LuCI, и локальных `rule_set` рядом с удалёнными, то есть второй формы
секции со своим хранением файлов. Пока её нет, список обязан быть доступен по
HTTP(S) — хотя бы с машины в самой локальной сети.

## 9. Валидация

К существующим проверкам (`routing-design.md`, раздел 8) добавляются:

- домен: только ASCII, `^\.?[A-Za-z0-9_-]+(\.[A-Za-z0-9_-]+)+$`, не длиннее 253
  символов. Схема, путь, `*`, пробел и кириллица отклоняются; для кириллических
  имён сообщение прямо просит punycode — преобразовать его на роутере нечем;
- `ip_cidr`: только IPv4, маска 0–32, голый адрес считается `/32`. IPv6
  отклоняется отдельным сообщением: он не маршрутизируется и в LAN не
  раздаётся, так что запись была бы мёртвой;
- `port`: 1–65535; диапазон `начало:конец`, начало не больше конца;
- `network`: пусто, `tcp` или `udp`;
- `rule_set`: имя существующей секции `ruleset`;
- у секции `ruleset`: `url` — только `http://` или `https://`, с разбором
  адреса, а не по одному префиксу; `update_interval` — пусто или число с
  суффиксом `h`/`d`;
- `main.ruleset_detour` — `direct` или имя существующей секции `outbound`;
  проверяется один раз на всю заявку, а не на каждый список;
- **у правила есть хотя бы одно условие любого вида.** Роль «всё остальное»
  по-прежнему принадлежит `main.final`;
- дубликаты внутри одного списка одного правила отклоняются, как сегодня
  отклоняются повторяющиеся MAC.

Правило, состоящее только из `port` и `network`, законно: «весь UDP на 443 —
заблокировать» — это осмысленный способ выключить QUIC, чтобы браузеры не
обходили снифинг.

Пересечения правил ошибкой не считаются и с доменными условиями становятся
нормой. Интерфейс показывает порядок явно и предупреждает о заведомо
недостижимом правиле — теперь это правило, полностью накрытое предыдущим по всем
трём группам.

Проверки после генерации не меняются: `sing-box check -c` на временном файле и
`nft -c -f` на временном ruleset. С появлением `sniff`, `rule_set` и новых полей
именно `sing-box check` остаётся последним арбитром совместимости (И5).

Чего валидация **не** проверяет — доступность списка по указанному адресу и его
пригодность для установленной версии sing-box. Ни то, ни другое бэкенду не
видно (8.1), и оба выясняются на запуске службы, где уже есть откат. Это
проверено, а не предположено: `check` принял конфигурацию со списком по
несуществующему адресу за нулевое время (15.1). Хорошая новость в том же факте —
валидация не ходит в сеть и остаётся дешёвой, так что `validate` может дальше
жить внутри синхронного RPC-вызова LuCI.

## 10. nftables

Не меняется ничего. Ни одна строка `nft_config()` не зависит от условий по
назначению: `zarap_prerouting` строится из имени LAN-интерфейса,
`zarap_guarded_v4` — из списков `client`, `zarap_protect_tproxy` — из константы
порта (И8). Тесты, закрепляющие это, перечислены в разделе 14.

## 11. Устройства: куда идёт трафик

`resolved_target` в его нынешнем виде — «первое правило, назвавшее устройство» —
с доменными условиями становится неправдой: правило может назвать устройство и
всё же не касаться большей части его трафика.

Поле разделяется на два:

- `resolved_target` — цель, куда трафик устройства уходит **по умолчанию**:
  первое правило, которое называет устройство и не имеет условий по назначению,
  а если такого нет — `main.final`;
- `matched_rules` — номера правил, которые устройства касаются, но только для
  части трафика.

Интерфейс показывает это как «напрямую, кроме правил 2 и 4». Одно значение в
колонке было бы враньём, а вранья в этой колонке быть не должно: она —
единственный ответ на вопрос «что будет с этим устройством».

`guarded` не меняется вовсе (И6): устройство, названное любым правилом, под kill
switch.

## 12. RPC-контракт

`status` дополняет каждое правило:

```json
{
  "rules": [
    {
      "clients": ["00:11:22:33:44:55"],
      "domains": ["youtube.com", ".googlevideo.com"],
      "ip_cidr": ["149.154.160.0/20"],
      "rule_sets": ["rs_1"],
      "ports": ["443"],
      "network": "",
      "target": "out_1"
    }
  ],
  "rulesets": [
    { "tag": "rs_1", "label": "Реклама", "url": "https://…",
      "update_interval": "1d", "in_use": true }
  ],
  "ruleset_detour": "out_1",
  "ruleset_detour_missing": false,
  "cache": { "size": 1258291, "free": 4194304 }
}
```

`cache` — размер `/etc/zarap/cache.db` и свободное место на `/overlay`, оба в
байтах (8.2). Ни размера отдельного списка, ни времени его обновления в ответе
нет: этого Zarap не знает, а показывать выдуманное число хуже, чем не
показывать никакого.

`validate` и `apply` принимают те же поля внутри элементов `rules` плюс список
`rulesets`:

```
args: { enabled: true, outbounds: [], rules: [], rulesets: [], clients: [], final: '' }
```

Замечание из кода остаётся в силе: rpcd строит политику ubus по *типу*
значения-образца, поэтому образцы обязаны быть настоящими значениями нужного
типа (`[]`, `''`, `true`).

Новый метод один — `refresh_rulesets`, без аргументов, за той же блокировкой,
что `apply`, и в `write` списка ACL: он перезапускает службу. Ответ говорит, что
именно произошло:

```json
{ "ok": true, "restarted": true, "cache": { "size": 2097152, "free": 18874368 } }
```

`restarted` отличает роутер с работающим Zarap от выключенного, где кэш просто
удалён до следующего запуска (8.3), а `cache` избавляет страницу от перезагрузки
ради новой строки о месте — вместе с ней уехали бы несохранённые правки. Отказ
приходит как `startup_error` с тем же указанием на журнал, что и откат
применения, и говорит, вернулся ли прежний кэш.

Добавление, изменение и удаление списка новых методов не требуют: это правка
секций, которую `apply` уже переписывает целиком, а загружать нечего.

Отдельно — уборка. Удаление последней секции `ruleset` оставляет `cache.db` с
содержимым, которое больше никому не нужно; `apply` в этом случае удаляет файл
вместе с отложенной копией, как он уже удаляет аренды, на которые не осталось
правил.

## 13. Интерфейс

### 13.1 Таблица правил

Колонка «Устройства» превращается в две: **«Откуда»** и **«Куда»**, обе
редактируются кнопкой, открывающей своё модальное окно.

```
#  Откуда              Куда                            Цель          
1  любое устройство    udp, порт 443                   заблокировать 
2  любое устройство    список «Реклама»                заблокировать 
3  Телевизор           youtube.com, .googlevideo.com   Нидерланды    
```

Пустая ячейка пишется словами — «любое устройство», «любой адрес», — а не
остаётся пустой: разница между «не выбрано» и «выбраны все» здесь ровно та, из-за
которой правило действует на весь дом.

Подпись под таблицей объясняет складывание условий одной фразой: применяется
первое подходящее правило; устройства и назначение соединяются через «и», а
внутри назначения домены, адреса и списки — через «или».

### 13.2 Окно «Куда»

Одно окно, три поля, каждое — многострочный ввод по записи на строку: домены,
диапазоны адресов, порты. Плюс список доступных `.srs` с отметками. Проверка
синтаксиса — на месте, по мере ввода, с той же формулировкой ошибки, которую
вернул бы бэкенд: круг запроса ради опечатки в домене никому не нужен.

Рядом с полем доменов — предупреждение, которое обязано быть там, пока этап 4 не
сделан: правило по домену действует на соединения, в которых видно имя, и не
действует на соединения к голому адресу; DNS-запросы при этом идут мимо прокси.

### 13.3 Списки правил

Отдельная секция на вкладке «Настройка»: таблица списков (метка, адрес
источника, интервал обновления, используется ли), кнопки «Добавить», «Удалить».
Под таблицей — строка «Скачивать списки через» с одним селектором подключения на
все списки сразу, строка про кэш: сколько занимает `cache.db` и сколько осталось
на `/overlay`, с предупреждением при малом остатке (8.2), — и кнопка «Обновить
списки сейчас».

Через что качать — вопрос, у которого один ответ на весь роутер: источники
списков заблокированы там же, где и всё остальное. В строке списка этого выбора
нет: десять списков означали бы десять раз один и тот же ответ и возможность
развести их между собой без причины.

Добавление ведёт себя ровно так же, как добавление подключения: страница кладёт
секцию в свой список, назначает `rs_<n>`, помечает «не сохранено», и правило
может сослаться на список сразу. На роутер — и, значит, в загрузку sing-box —
он попадает при применении.

Кнопка обновления идёт за тем, что уже применено: sing-box качает конфигурацию,
которую ему отдали, а не строки, набранные на странице. Поэтому она смотрит на
применённые списки, а не на свои: без них она выключена, а при расхождении под
ней стоит строка о том, что обновится применённое. Подпись рядом говорит
остальное — что обновятся все списки сразу, что связь прервётся на несколько
секунд и что при неудаче вернётся прежний кэш; при выключенном Zarap — что кэш
очистится, а списки скачаются при следующем запуске.

Ответ приходит уведомлением, страница не перезагружается: размер кэша приезжает
вместе с ответом, а перезагрузка стоила бы несохранённых правок. Кнопка на время
запроса говорит «Обновление…» и возвращается сама — попробовать ещё раз можно
сразу.

Поля добавления — адрес, метка и интервал. Общий селектор загрузки при этом
поднимается с `direct` на первое подключение, когда добавляется первый список:
источник списка обычно заблокирован там же, где и всё остальное. Дальше выбор
принадлежит пользователю, и следующий список его уже не трогает.

Удалённое подключение, на которое селектор всё ещё показывает, названо ошибкой
под ним самим — обновляться перестанут все списки сразу. Значение при этом не
подменяется на `direct`: это увело бы загрузку туда, где источник и
заблокирован, молча.

Удаление списка запрещено, пока на него ссылается правило, и отказ называет, кто
именно ссылается — как это уже сделано для подключений.

Что интерфейс обязан сказать о первом применении: пока список не скачан ни разу,
запуск Zarap зависит от доступности адреса, и при неудаче применение
откатывается целиком (8.2). Это единственное место, где добавление списка
отличается от добавления подключения по последствиям, и умалчивать о нём
нельзя.

### 13.4 Диалоги

К двум существующим подтверждениям добавляется третье: **правило без устройств с
целью `block`**. Оно оставляет без названного назначения весь дом, и это стоит
сказать прямо до сохранения, а не после звонка от домашних.

## 14. Тесты

К `tests/test_route_mapping.py`:

- одна секция с доменом, адресом и списком даёт три подряд идущих элемента с
  одной целью и одинаковым `source_ip_cidr`;
- `youtube.com` даёт и `domain`, и `domain_suffix`; `.googlevideo.com` — только
  `domain_suffix`;
- правило без `client` не даёт `source_ip_cidr` вовсе (а не пустой массив);
- `sniff` выпускается тогда и только тогда, когда есть доменное условие или
  список, и стоит первым;
- `rule_set` объявляется удалённым, с `url` и `update_interval` из секции, и
  только для списков, на которые ссылаются;
- `download_detour` есть при `ruleset_detour 'out_1'`, отсутствует при `direct`
  и одинаков во всех объявлениях;
- `experimental.cache_file` выпускается тогда и только тогда, когда объявлен
  хотя бы один список;
- `target: block` даёт `action: reject` в каждом элементе группы;
- порядок групп совпадает с порядком секций.

К `tests/test_nft_rules.py` — закрепление И8:

- `nft_config()` совпадает побайтово при добавлении доменных условий, условий по
  адресам, портам и спискам;
- набор kill switch не меняется от добавления правила без устройств (И7);
- устройство, названное правилом с доменным условием, в наборе есть (И6).

Новый `tests/test_rule_conditions.py` — валидация: домены с кириллицей, схемой,
`*`, путём; IPv6 в `ip_cidr`; порт 0 и 65536; диапазон наоборот; ссылка на
несуществующий список; `url` не по HTTP; `ruleset_detour` на несуществующее
подключение; правило без условий; правило только с портом.

`tests/test_contract.py` — новые образцы `args`, поля `rulesets` и `cache` в
`status`, отсутствие секретов в новых полях. Отдельная проверка: удаление
последней секции `ruleset` убирает `cache.db`. Для `refresh_rulesets` — что
метод объявлен, что каждый метод RPC назван в ACL и что принудительное
обновление откладывает кэш и возвращает его на место.

Браузерные тесты — окно «Куда», состав `apply` с новыми полями, отказ удалить
список, на который ссылаются, предупреждение о правиле `block` без устройств и
предупреждение о первом применении несохранённого списка. Для обновления —
удачное обновление с новым размером кэша без перезагрузки страницы, выключенный
Zarap, отказ с возвращённым кэшем и кнопка, которая смотрит на применённые
списки, а не на набранные.

## 15. Что проверить на роутере до реализации

Проверки статические, как в разделе 14 `routing-design.md`: конфигурации
отдаются `sing-box check`, состояние читается через ubus и uci. Каждый пункт
может изменить решение, поэтому проходить их надо до кода, а не после.

Схемные проверки делаются так: конфигурация из соответствующего раздела
кладётся во временный файл и отдаётся `sing-box check`. Ничего не применяется и
не перезапускается. Проверки, которым нужна живая служба, вынесены в 15.3
отдельно: каждая из них на время оставляет дом без интернета.

### 15.1 Схема: проверено

OpenWrt 25.12.5 (r33051), sing-box 1.12.17, подкоманда `rule-set` на месте.
Ничего не применялось.

| Что | Результат |
| --- | --- |
| Условия по адресам, портам и протоколу | Принято. `source_ip_cidr`, `ip_cidr`, `port`, `port_range`, `network` и `action: "reject"` в одном наборе правил — то, что генератор выпускает после шага 1 |
| `action: "sniff"` и условия по доменам | Принято, вместе с `domain` и `domain_suffix` в одном правиле. Шаг 2 разблокирован |
| `sniff` с явным `timeout` | Принято, то есть ожидание заголовка настраиваемо, а не только умолчание |
| Удалённый `rule_set` с `download_detour`, `update_interval` и `cache_file` | Принято целиком. Раздел 8 схемой подтверждён |
| Локальный `rule_set` | Принят, но `check` **открывает и разбирает** файл: по несуществующему пути он падает с `parse rule-set[0]: no such file`. Для запасного пути это значит, что валидация потребует файла на диске — ещё один довод за удалённые списки |
| `direct` inbound + `action: "hijack-dns"` | Принято. Слушатель DNS для этапа 4 существует в этой версии — главный риск раздела 7 снят |
| `dns.servers`, типизированная форма (1.12+) | Принята вместе с `strategy: "ipv4_only"` и `dns.rules` |
| `dns.servers`, старая форма (`address`) | Тоже принята; генератор пишет типизированную, а И5 поймает разницу, если она появится |
| Ходит ли `check` в сеть | Нет: конфиг со списком по нероутируемому адресу проверен за 0 с с кодом 0. `validate` остаётся дешёвым и синхронным |
| Место | `/overlay` — 44 МиБ всего, 21,8 МиБ свободно (48 % занято) |

Два следствия, которые меняют текст, а не решения.

**Недоступный список валидацией не ловится совсем.** `check` принял конфигурацию
со списком по адресу, которого нет, не заметив этого. Раздел 9 предполагал
именно это, но теперь это проверено: единственное место, где неверный адрес
проявится, — запуск службы, то есть откат (8.2).

**Порог предупреждения о месте.** 10 % от 44 МиБ — это 4,4 МиБ, то есть
предупреждение придёт, когда для geosite-набора уже поздно. Порог правится на
абсолютный: предупреждать, когда свободного меньше 8 МиБ, и всё равно показывать
размер кэша рядом. Настоящий размер набора остаётся неизмеренным — он в 15.2.

### 15.2 Снифинг: проверено

Пробой, не задевшей ни службу, ни домашний трафик: отдельный процесс sing-box на
loopback, вход `direct` с `override_address` (назначение задано адресом, как при
захвате TProxy), три выхода-близнеца с разными тегами и вместо клиента — TLS
ClientHello, собранный побайтно и отправленный через `nc`. Два прогона,
различающиеся только порядком правил:

```
router: match[0] inbound=probe-in => sniff
router: sniffed protocol: tls, domain: example.com
outbound/direct[by_ip]: outbound connection to 8.6.112.0:443      ← ip_cidr первым
outbound/direct[by_domain]: outbound connection to 8.6.112.0:443  ← домен первым
```

Отсюда три факта.

**Снифинг работает и даёт имя** — доменные условия применимы (шаг 2
разблокирован).

**Адрес назначения снифинг не съедает.** Правило по `ip_cidr` совпало, стоя
после `sniff`. Значит порядок групп внутри правила (5.1) свободен: адреса и
домены можно писать в любом порядке, и разбивать одну секцию на группу
подряд идущих правил по-прежнему корректно.

**Наружу уходит адрес, а не имя.** `outbound connection to 8.6.112.0:443` — в
outbound уезжает тот адрес, который клиент получил от DNS; имя остаётся только
для маршрутизации. Это опровергает то, что раздел 6 утверждал до проверки, и
переписано там же.

Третий прогон проверял, нельзя ли это поведение переключить: на вход пробы
добавлено `sniff_override_destination`, то самое поле, которое в старой схеме
подменяло назначение именем. Конфигурация принята — и ничего не изменилось,
наружу снова ушёл адрес. Поле объявлено устаревшим и в этой версии не работает,
так что ручки, отправляющей имя, нет: **резолв придётся чинить резолвом**
(этап 4), а не маршрутизацией.

Оговорка к измерению: вход пробы — `direct` с `override_address`, то есть
назначение задано адресом принудительно. Это и есть положение TProxy-соединения,
ради которого проверка ставилась, но на inbound'ах, куда клиент передаёт имя
сам (socks5h, HTTP CONNECT по имени), поведение может быть другим — Zarap таких
входов не имеет.

### 15.3 Живая служба: ещё не проверено

Каждая на время оставляет дом без интернета, поэтому момент выбирается вручную:
остановить службу, подменить нужное в `/etc/zarap/sing-box.json`, запустить и
прочитать `logread -e sing-box` вместе с `ss -lntup`.

| Что | Почему это важно |
| --- | --- |
| Что делает старт без кэша и с недоступным адресом | Определяет, срабатывает ли откат (8.2) и что писать в сообщении. Если sing-box стартует и работает без списка — риск 6 мягче, чем записан, и вместо текста ошибки нужно предупреждение на странице. От этого же зависит, что вправе утверждать принудительное обновление: поднявшаяся без кэша служба означает скачанные списки только в том случае, если без них она не поднимается (8.3) |
| Переживает ли кэш перезагрузку | Ради этого `cache.db` и лежит в `/etc/zarap/` |
| Размер `cache.db` после настоящего geosite-набора | От него зависит, годится ли порог в 8 МиБ и стоит ли вообще предлагать большие наборы на 44-мегабайтном разделе |
| Что делает sing-box, когда кэш некуда писать | Остаётся ли переполненный flash предупреждением или становится отказом запуска (8.2) |
| Пересылка по домену в dnsmasq (этап 4) | Подтверждает, что 7.2 не требует переписывать резолвер целиком |

### 15.4 Bootstrap: проверено

Пробой на настоящем sing-box 1.12.17 — не `check`, а запуск: вход `socks` на
loopback вместо TProxy, выход `out_1` с доменом в адресе сервера, конфиг во всём
остальном тот, что выпускает генератор.

| Что | Результат |
| --- | --- |
| Домен в адресе сервера с `dns.final` в туннель (как было до 7.6) | Петля. `dns: lookup domain nl.example.net` → `outbound connection to 1.1.1.1:443` через `out_1` → снова `lookup domain nl.example.net`, и так без конца. Соединение висит, в журнале ни одной ошибки |
| Домен в адресе сервера без секции `dns` вовсе | Работает: sing-box берёт системный резолвер. Это то самое устаревшее поведение, ради которого и понадобился `default_domain_resolver` |
| `dns.servers[].detour: "direct"` | Отвергнуто на старте: `start dns/https[dns_bootstrap]: detour to an empty direct outbound makes no sense` |
| Тот же сервер без `detour`, при `route.final: "out_1"` | Запрос ушёл напрямую, не через `out_1`. Значит сервер без `detour` не наследует `route.final`, и прямой outbound объявлять не требуется |
| `route.default_domain_resolver` строкой с тегом | Принято. Резолв пошёл прямо в названный сервер: `dns.rules` не просматривались, `dns.final` в туннель не сработал |
| Резолв и дальше по цепочке | `lookup succeed for example.com`, дальше соединение дошло до `reality verification failed` — то есть до самого сервера, чего до 7.6 не случалось |
| `sing-box check` на всех трёх формах конфига (с доменными правилами, без них, со списком напрямую) | Принято, код 0 |

Оговорка к измерению: DoH проверялся в песочнице с перехватом TLS, поэтому
проверка сертификата `1.1.1.1` там падает (`doesn't contain any IP SANs`).
Маршрут при этом виден целиком и от шифрования не зависит; тот же конфиг с
обычным DNS вместо DoH резолвит до конца. На роутере сертификат Cloudflare
содержит IP SAN, и это остаётся единственным непроверенным звеном.

## 16. Риски

1. **Снифинг — не гарантия, и лечит он только половину.** Доменное правило молча
   не срабатывает на трафике, где имени не видно, и трафик уходит по `final`.
   Хуже того, проверка (15.2) показала, что наружу уезжает адрес, полученный от
   провайдерского DNS: правило меняет путь, но не точку назначения, поэтому
   против блокировки, устроенной в DNS, оно бессильно. Молчаливость — худшая
   часть: пользователь видит правило и считает, что оно работает. Смягчается
   текстом в интерфейсе (13.2), а по существу — только этапом 4.
2. **Правило без устройств никого не защищает.** Форма правила, которая
   выглядит сильнее прежней, даёт меньше гарантий (И7). Интерфейс обязан
   различать эти два случая, иначе kill switch перестанет быть понятным.
3. **Кэш списков занимает flash.** Роутер — не сервер, `/overlay` небольшой, а
   geosite целиком в разы больше того, что нужно дому. Ограничить размер Zarap
   не может — файл не его, — поэтому остаётся показывать размер кэша и
   свободное место и предупреждать заранее (8.2).
4. **Разбор правил стоит процессорного времени.** Каждое соединение проходит
   список правил, а `rule_set` — это ещё и поиск по набору из десятков тысяч
   элементов. При захвате всего LAN это делается для всего дома. Замерять не
   планируется, как и в риске 1 `routing-design.md`.
5. **Этап 4 добавляет sing-box в путь резолва.** Для названных доменов, но
   всё-таки: при остановленном sing-box эти имена перестают резолвиться совсем.
   Именно поэтому пересылается названное, а не весь DNS (7.3).
6. **Запуск зависит от чужого сервера.** Пока список не попал в кэш, старт
   sing-box требует доступности его адреса, а неудача — это откат применения
   (при первом применении) или неподнявшаяся служба (после чистки кэша или
   переустановки). Это плата за то, что списками занимается sing-box, и она
   принята: своя загрузка ходила бы за файлом мимо прокси, то есть ровно туда,
   где источник и заблокирован (8.1). Смягчается кэшем на постоянном разделе,
   загрузкой через подключение и внятным текстом ошибки; не смягчается ничем при
   первом применении. Чистка кэша — единственный случай, который удалось убрать
   совсем: принудительное обновление кэш откладывает и возвращает на место, если
   без него служба не поднялась (8.3). Валидация тут не поможет по построению: `check` принимает
   недоступный адрес не глядя, и это проверено (15.1). Насколько риск серьёзен —
   отказывается ли sing-box стартовать или работает без списка — пока не
   проверено (15.2).
7. **Формат `.srs` версионируется вместе с sing-box.** Список, собранный для
   более новой версии, установленный бинарник не прочтёт, и Zarap не может это
   ни предсказать, ни объяснить: файла он не видит. Проявится отказом запуска,
   разбирается по журналу (8.3).

## 17. Этапы

**Этап 3 — условия по назначению.** Схема UCI, генератор, валидация, RPC,
интерфейс, тесты. Внутри порядок такой:

1. **`ip_cidr`, `port`, `network`. Сделан.** Валидация, генератор с группой
   правил (5.1), чтение и запись новых `list` в UCI, `device_routing` вместо
   `resolved_target` (раздел 11) и тесты на всё перечисленное. Страница пока не
   редактирует эти условия, но показывает их и отдаёт обратно нетронутыми —
   иначе первое же сохранение из LuCI стирало бы то, что вписано руками;
2. **Домены и `action: "sniff"`. Сделан.** Одна запись на домен (4.2), группа
   правил на секцию (5.1), `sniff` только при доменном условии (5.2), проверка
   ввода с отдельными отказами на схему, путь, звёздочку и кириллицу. Страница
   их показывает и отдаёт обратно нетронутыми, как и остальные условия;
3. **Секции `ruleset`, удалённый `rule_set`, `cache_file`. Сделан.** Набор
   объявляется только когда на него ссылаются, `download_detour` опускается
   при `direct`, кэш выпускается вместе с первым набором и удаляется, когда
   исчезает последний. `status` отдаёт наборы, размер кэша и свободное место, а
   также отмечает удалённое подключение, выбранное для загрузки списков;
4. **Сообщение об откате. Сделано.** При объявленных списках отказ запуска
   называет их и говорит, что причина есть в журнале (8.2);
5. **Интерфейс. Сделан.** Две колонки условий, окно «Куда» с проверкой ввода
   теми же словами, что и на роутере, секция списков с кэшем и остатком места,
   подтверждение блокировки для правила без устройств;
6. **Браузерные тесты. Сделаны.**
7. **Принудительное обновление списков. Сделано.** `refresh_rulesets`
   откладывает кэш, перезапускает sing-box и возвращает прежний кэш, если без
   него служба не поднялась; кнопка под таблицей списков идёт за применёнными
   списками и обновляет их все сразу (8.3).

Первые три шага — целиком под ucode, где ошибка стоит дёшево. Схемные проверки
пройдены (15.1), и шаги 2 и 3 ими разблокированы; из оставшихся (15.2) шаг 2
ждёт только одну — переживает ли `ip_cidr` снифинг, потому что от неё зависит
порядок групп внутри правила. Шаг 4 ждёт проверок про кэш и старт без него: от
их исхода зависит и текст сообщения, и формулировки рисков 6 и 7.

**Этап 4 — DNS-маршрутизация. Сделан.** Слушатель `zarap-dns` на
`127.0.0.1:5353` с правилом `hijack-dns`, по DoH-серверу на каждое подключение,
которому правило отправляет домены, `strategy: "ipv4_only"`, адресная пересылка
из dnsmasq и её уборка при удалении пакета.

Включается само и только для названных доменов, отправленных в подключение: у
`direct` резолв через туннель ничего не меняет, у `block` соединение всё равно
не состоится, а весь DNS дома через прокси — это потеря имён целиком при
остановленном sing-box (7.3). Своей строкой пересылки считается та, что
указывает на наш порт: отдельного поля для пометки у элемента списка нет, а
чужие строки трогать нельзя.

Страница говорит, сколько имён резолвится через прокси, — и что доменные
правила без этого защищают только от DPI.

Его вес после проверки изменился. Планировался он как приватность и запас
прочности, а оказался второй половиной доменных правил: без него правило по
домену справляется с DPI по SNI и не справляется с блокировкой в DNS (6, 15.2).
Этап 3 остаётся полезен сам по себе — маршрут по спискам и адресам, выбор
выхода для домена, блокировка рекламы, — но пока этап 4 не сделан, интерфейс
обязан говорить, чего доменное правило не умеет, а не оставлять пользователя с
правилом, которое выглядит работающим.

**Этап 5 — группы outbound'ов.** `selector` и `urltest`, как записано в 13.3
`routing-design.md`. Не зависит от этого документа.

## Источники

- [Rule Action — sing-box](https://sing-box.sagernet.org/configuration/route/rule_action/)
- [Route Rule — sing-box](https://sing-box.sagernet.org/configuration/route/rule/)
- [Rule Set — sing-box](https://sing-box.sagernet.org/configuration/rule-set/)
- [DNS Server — sing-box](https://sing-box.sagernet.org/configuration/dns/server/)
- [DNS Rule — sing-box](https://sing-box.sagernet.org/configuration/dns/rule/)
- [Deprecated — sing-box](https://sing-box.sagernet.org/deprecated/)
- [Migration — sing-box](https://sing-box.sagernet.org/migration/)
